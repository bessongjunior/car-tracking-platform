FLASK_ENV=development

# Database — uses peer auth (OS user juniorbesong)
DATABASE_URL=postgresql:///cartracker

# JWT
SECRET_KEY=dev-cartracker-secret-change-in-prod
JWT_SECRET_KEY=dev-jwt-cartracker-change-in-prod
JWT_ACCESS_TOKEN_EXPIRES=3600

# Firebase project: emma-smart-traffic
# Download serviceAccountKey.json from:
#   Firebase Console → Project settings → Service accounts → Generate new private key
FIREBASE_DATABASE_URL=https://emma-smart-traffic-default-rtdb.firebaseio.com
FIREBASE_CREDENTIALS=serviceAccountKey.json

# OTP
OTP_EXPIRY_SECONDS=300

# SMTP (Gmail App Password — used for OTP emails)
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=juniorbesong8@gmail.com
SMTP_PASSWORD=xdfb xpbc hjoz mahw
SMTP_STARTTLS=true
SMTP_DOMAIN=gmail.com
